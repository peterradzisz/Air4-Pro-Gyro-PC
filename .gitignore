__pycache__/
*.pyc
*.pyo
*.exe
.env
test_output.txt

# Runtime/local
.omo/
nul

# Runtime files
airpin.log
airpin_settings.json
*.lnk

# venv (created by setup.bat)
.venv/

# Screenshots: ignore user captures (Ctrl+Alt+X output), curated README thumbs live in docs/img/
screenshots/

# DLLs are now bundled in lib/ and tracked in git

# Portable Python (built by build_portable.bat, ~190MB - distributed via Releases, not git)
python/
python-embed.zip
